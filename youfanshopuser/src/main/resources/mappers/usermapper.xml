<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youfan.mapper.UserMapper">

    <select id="findUserInfo" resultType="com.youfan.model.User">
        select name, age, address, telephone, qq, weixin, email, sex, birthday from user;
    </select>

    <insert id="inseruserInfo" parameterType="com.youfan.model.User">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
            select LAST_INSERT_ID()
        </selectKey>

        insert into user (name, account, age, passwordencrypt, address, telephone, qq, weixin, email, sex, birthday)
        value(#{name}, #{account}, #{age}, #{passwordencrypt}, #{address}, #{telephone}, #{qq}, #{weixin}, #{email}, #{sex}, #{birthday})
    </insert>
    
    <update id="updateUser" parameterType="com.youfan.model.User">
        update user set name=#{name}, age=#{age}, address=#{address}, qq=#{qq}, weixin=#{weixin}, email=#{email},
        sex=#{sex}, birthday=#{birthday}, account=#{account} where id=#{id}
    </update>

    <select id="findByUserid" parameterType="int" resultType="com.youfan.model.User">
        select * from user where id = #{value}
    </select>



</mapper>